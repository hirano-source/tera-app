# P1 詳細設計（共有Webアプリ＋可視化）

作成日: 2026-06-19
対象: マルチテナント共有・可視化（[設計_マルチテナント.md](./設計_マルチテナント.md) のPhase 1）
前提: 完全無料構成（Cloudflare Workers/Pages＋Supabase無料）。AIは各自のClaude。
※本書は設計のみ。デプロイは別途GO（掟 第3条）。

---

## 1. データモデル（ER）

```
users ──< memberships >── workspaces
                              │（全テーブルに workspace_id）
   ┌──────────┬──────────┬────┴─────┬───────────┬──────────────┐
 goals      tasks     activities  comments  notifications  calendar_sources
(owner,    (assignee, (actor,     (author,  (recipient)    (user)
 parent,    status,    type,       target)
 progress)  goal)      summary)
```

原則: **全テーブルに `workspace_id`**。閲覧は「そのworkspaceにmembershipがある人」のみ（行レベルセキュリティ＝RLS）。

---

## 2. DBスキーマ（PostgreSQL DDL）

```sql
-- 列挙型
create type role        as enum ('owner','admin','member');
create type task_status as enum ('todo','doing','done','blocked');
create type goal_status as enum ('active','done','archived');
create type task_source as enum ('goal','calendar','manual');

-- ユーザー（Supabase auth.users と1:1。表示用情報のみ保持）
create table users (
  id          uuid primary key,            -- = auth.users.id
  name        text not null,
  email       text not null,
  avatar_color text default '#6d5dfc',
  created_at  timestamptz default now()
);

-- ワークスペース（テラ / スキルスリー …）
create table workspaces (
  id         uuid primary key default gen_random_uuid(),
  name       text not null,
  created_at timestamptz default now()
);

-- 所属＋ロール
create table memberships (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  user_id      uuid not null references users(id)      on delete cascade,
  role         role not null default 'member',
  created_at   timestamptz default now(),
  unique (workspace_id, user_id)
);

-- ゴール（7年→1年→月→… の階層）
create table goals (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  owner_id     uuid references users(id),
  parent_id    uuid references goals(id) on delete cascade,
  title        text not null,
  horizon      text,                       -- '5y','1y','month' など
  current      text default '',            -- 現状
  criteria     text default '',            -- 完了の基準
  progress     int  default 0 check (progress between 0 and 100),
  due_date     date,
  status       goal_status default 'active',
  created_at   timestamptz default now(),
  updated_at   timestamptz default now()
);
create index on goals (workspace_id);
create index on goals (parent_id);

-- タスク（誰が何を・状態）
create table tasks (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  goal_id      uuid references goals(id) on delete set null,
  assignee_id  uuid references users(id),
  title        text not null,
  status       task_status default 'todo',
  for_date     date,
  is_today     boolean default false,
  source       task_source default 'manual',
  due_date     date,
  created_at   timestamptz default now(),
  updated_at   timestamptz default now()
);
create index on tasks (workspace_id);
create index on tasks (assignee_id);
create index on tasks (goal_id);

-- 活動記録（誰が・いつ・何をした）
create table activities (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  actor_id     uuid references users(id),
  ts           timestamptz default now(),
  type         text not null,              -- task_completed, goal_updated, note...
  goal_id      uuid references goals(id) on delete set null,
  task_id      uuid references tasks(id) on delete set null,
  summary      text not null
);
create index on activities (workspace_id, ts desc);

-- コメント（経緯・詰まり）
create table comments (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  target_type  text not null,              -- 'goal' | 'task'
  target_id    uuid not null,
  author_id    uuid references users(id),
  body         text not null,
  created_at   timestamptz default now()
);

-- 通知
create table notifications (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  user_id      uuid not null references users(id),   -- 受信者
  type         text not null,
  payload      jsonb,
  read         boolean default false,
  created_at   timestamptz default now()
);

-- カレンダー連携（iCal URL）
create table calendar_sources (
  id           uuid primary key default gen_random_uuid(),
  workspace_id uuid not null references workspaces(id) on delete cascade,
  user_id      uuid not null references users(id),
  ical_url     text not null,
  created_at   timestamptz default now()
);
```

### マルチテナント分離（RLS）
```sql
alter table goals enable row level security;
create policy goals_in_my_workspace on goals
  using (workspace_id in (
    select workspace_id from memberships where user_id = auth.uid()
  ));
-- 同型のポリシーを tasks / activities / comments / notifications / calendar_sources にも適用
```
→ テラのデータはスキルスリーから物理的に見えない。

---

## 3. 権限マトリクス

| 操作 | owner | admin | member |
|---|:---:|:---:|:---:|
| ワークスペース設定・課金 | ✓ | – | – |
| メンバー招待・ロール変更 | ✓ | ✓ | – |
| 全員のゴール/タスク閲覧 | ✓ | ✓ | ✓ |
| タスクを他人に割当 | ✓ | ✓ | △(自分の範囲) |
| 自分のゴール/タスク作成・更新 | ✓ | ✓ | ✓ |
| 他人のゴール削除 | ✓ | ✓ | – |
| コメント | ✓ | ✓ | ✓ |

---

## 4. API設計（Cloudflare Workers / Hono）

認証: `Authorization: Bearer <Supabase JWT>`。`:wid` は所属チェック必須。

| メソッド | パス | 用途 |
|---|---|---|
| GET | `/me` | 自分＋所属ワークスペース一覧 |
| POST | `/workspaces` | ワークスペース作成 |
| POST | `/workspaces/:wid/invite` | メンバー招待 |
| GET | `/workspaces/:wid/members` | メンバー＋ロール一覧 |
| GET | `/workspaces/:wid/dashboard` | **可視化：メンバー別の進捗・担当・blocked** |
| GET/POST | `/workspaces/:wid/goals` | ゴール一覧/作成（`?horizon&parentId`）|
| GET/PATCH/DELETE | `/goals/:id` | ゴール詳細/更新/削除 |
| GET | `/goals/:id/chain` | ゴール階層（今日→月→年→…）|
| GET/POST | `/workspaces/:wid/tasks` | タスク一覧/作成（`?assignee&status&date&today`）|
| PATCH | `/tasks/:id` | 状態・タイトル更新 |
| POST | `/tasks/:id/assign` | 担当割当（→相手に通知）|
| GET/POST | `/workspaces/:wid/activities` | 活動フィード |
| GET/POST | `/workspaces/:wid/notifications` | 通知 |
| GET/POST/DELETE | `/workspaces/:wid/calendar-sources` | iCal URL管理 |

---

## 5. 画面設計（既存UI＋新規）

既存（ローカル版）をマルチテナント化＋1画面追加：

| 画面 | 変更点 |
|---|---|
| 上部バー | **ワークスペース切替**（テラ / スキルスリー）を追加 |
| 実行（今日のToDo） | 自分の今日のタスク（`assignee=me`）|
| ゴール | ワークスペースのゴール階層＋**進捗バー** |
| **チームダッシュボード（新規）** | **メンバー別カード：今やっていること / 進捗% / blocked / 最終活動**。これが「誰が何をどこまで」の核 |
| メンバー | 一覧・ロール・招待 |
| チャット | ゴール紐付けスレッド（既存）＋ワークスペース絞り |
| 通知 | 割当・コメント等の受信 |
| 設定 | プロフィール・カレンダー連携・**MCP接続URL表示**（P2用）|

### チームダッシュボード（ワイヤー）
```
┌── テラ ▼ ─────────────────── 検索 ──────────── 🪙0 ─┐
│  チームの状況                                          │
│ ┌─ 鈴木 ───────┐ ┌─ 山田 ───────┐ ┌─ 田中 ──────┐ │
│ │ 提案資料作成   │ │ 競合調査      │ │ LP構成      │ │
│ │ ▓▓▓▓▓░ 80%   │ │ ▓▓░░░░ 30% ⛔│ │ ▓▓▓░░ 55%  │ │
│ │ 最終: 2h前    │ │ blocked:資料待ち│ │ 最終: 朝     │ │
│ └──────────────┘ └──────────────┘ └─────────────┘ │
└────────────────────────────────────────────────────┘
```

---

## 6. MCPツール仕様（テナント対応・P2接続）

OAuthトークンが `(user, workspace, role)` を識別。全ツールが自動でそのworkspaceに絞り込み。

| ツール | 入力 | 役割 |
|---|---|---|
| `get_context` | – | 自分・ワークスペース |
| `list_goals` | horizon?, parentId? | ゴール（階層）|
| `get_goal` | goalId | 詳細＋階層chain |
| `create_goal` | title, parentId?, horizon?, criteria? | ゴール作成 |
| `list_tasks` | assignee?, status?, date?, todayOnly? | タスク（自分/他人）|
| `create_task` | title, goalId?, assignee?, isToday? | タスク作成 |
| `update_task` | taskId, status?, title? | 状態更新 |
| `assign_task` | taskId, to | **他メンバーに割当→相手のClaudeが受け取る** |
| `list_members` | – | メンバー一覧 |
| `get_member_progress` | memberId | **「誰が何をどこまで」** |
| `list_team_activity` | limit? | チーム活動フィード |
| `get_activity_log` / `log_activity` | … | 自分の活動記録 |

プロンプト: `plan_goal`（葬式→価値観→7年→今日）はそのまま継承。

---

## 7. 実装順序（P1内）

1. Supabaseプロジェクト＋スキーマ（上記DDL）＋RLS
2. Cloudflare Workers に Hono API（認証ミドルウェア→各エンドポイント）
3. 既存React UIを API接続に切替＋ワークスペース切替＋**チームダッシュボード**追加
4. Cloudflare Pages 配信（※公開＝デプロイはGO後）

ローカル開発（`wrangler dev` ＋ Supabaseローカル/無料プロジェクト）まではGes不要で進められる。
